/*
	The configuration here is the same as for sub1.  Any number of subprojects can incorporate the definitions that
	produces these features with a relatively small amount of local configuration.
 */

plugins {
	id 'java'
	id 'org.jetbrains.kotlin.jvm'
}

apply from: kotlintestScript
